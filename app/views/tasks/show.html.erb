<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Title</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
<tr>
  <td><%= @task.id %></td>
  <td><%= @task.name %></td>
  <td><%= @task.body %></td>
</tr>
</tbody>
</table>
<div>
<ul>

  <%= link_to "Back", category_path %> |

  <%= link_to "Edit Task", edit_category_task_path(@task) %>

  <%= button_to 'Delete', category_task_path(@task), confirm: 'Are you sure?', method: :delete %>
</div>